app-id: com.burnttoasters.conv2
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: '24.08'
sdk-extensions:
  - org.freedesktop.Sdk.Extension.node22
command: conv2
separate-locales: false

finish-args:
  - --share=ipc
  - --socket=x11
  - --socket=wayland
  - --socket=pulseaudio
  - --device=dri
  - --share=network
  - --filesystem=home
  - --filesystem=xdg-videos
  - --filesystem=xdg-download
  - --filesystem=/tmp
  - --env=ELECTRON_OZONE_PLATFORM_HINT=auto

modules:
  - name: conv2
    buildsystem: simple
    build-options:
      append-path: /usr/lib/sdk/node22/bin
      build-args:
        - --share=network
      env:
        ELECTRON_SKIP_BINARY_DOWNLOAD: '1'
        npm_config_cache: /run/build/conv2/flatpak-node/npm-cache
        HUSKY: '0'
    build-commands:
      - npm ci --legacy-peer-deps
      - npm run build
      - mkdir -p /app/lib/conv2
      - mkdir -p /app/lib/conv2/resources/ffmpeg
      - cp -r dist /app/lib/conv2/
      - cp -r assets /app/lib/conv2/
      - cp -r node_modules /app/lib/conv2/
      - cp package.json /app/lib/conv2/
      - cp licenses.json /app/lib/conv2/
      - |
        FF_ARCH_DIR=x64
        if [ "$FLATPAK_ARCH" = "aarch64" ]; then FF_ARCH_DIR=arm64; fi
        cp -r "resources/ffmpeg/linux/$FF_ARCH_DIR"/* /app/lib/conv2/resources/ffmpeg/
      - cp resources/ffmpeg/NOTICE.txt /app/lib/conv2/resources/ffmpeg/NOTICE.txt
      - cp resources/ffmpeg/ffmpeg_license.txt /app/lib/conv2/resources/ffmpeg/LICENSE.txt
      - cp resources/ffmpeg/SOURCE_OFFER.txt /app/lib/conv2/resources/ffmpeg/SOURCE_OFFER.txt
      - install -Dm755 conv2.sh /app/bin/conv2
      - install -Dm644 com.burnttoasters.conv2.desktop /app/share/applications/com.burnttoasters.conv2.desktop
      - install -Dm644 com.burnttoasters.conv2.metainfo.xml /app/share/metainfo/com.burnttoasters.conv2.metainfo.xml
      - install -Dm644 assets/icon.png /app/share/icons/hicolor/256x256/apps/com.burnttoasters.conv2.png
      - install -Dm644 assets/icon.png /app/share/icons/hicolor/512x512/apps/com.burnttoasters.conv2.png
    sources:
      - type: dir
        path: .
